# poc-cloud-automated-testing
Trigger automated tests in a cloud environment (GitHub Actions) whenever code is pushed to a specific branch. The PoC must be low‑cost, quick to build (&lt; 2 hours), and provide clear pass/fail evidence.
